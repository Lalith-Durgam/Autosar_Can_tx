#include<stdio.h>
#include"can_if.h"
#include"can_if_config.h"
#include"pdur_config.h"
#include"pdur.h"
#include"can_tx.h"
void can_if(msg1)
{
    can_if_config(msg_type msg1);
    can_tx_drv(msg_1_send);
   // can_drv()
}
